import { Title as MantineTitle } from '@mantine/core';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <>
      <MantineTitle order={2}>Home page</MantineTitle>
    </>
  );
};

export default Home;
