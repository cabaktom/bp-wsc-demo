import { Title as MantineTitle } from '@mantine/core';
import type { NextPage } from 'next';

const Programme: NextPage = () => {
  return (
    <>
      <MantineTitle order={2}>Programme page</MantineTitle>
    </>
  );
};

export default Programme;
